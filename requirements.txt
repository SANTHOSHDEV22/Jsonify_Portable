# ============================================================
# Jsonify - runtime dependencies
#
#   pip install -r requirements.txt
#
# Development tools:  requirements-dev.txt
# Production builds:  requirements-build.txt
# (pyproject.toml is the source of truth; a test keeps this file in sync.)
# ============================================================

# Desktop UI
PySide6>=6.6

# JSON tooling
jsonpath-ng>=1.7
jsonschema>=4.23
PyYAML>=6.0

# HTTP client (API client, update checks)
httpx>=0.28
